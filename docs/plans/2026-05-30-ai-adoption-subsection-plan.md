# AI Adoption Subsection Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add an `AI Adoption` subsection to the Stats Perform Engineering Manager section of the CV.

**Architecture:** This is a content-only change in the markdown source of truth. Insert a new subsection with 2 bullets into `content/cv.md`, rebuild the generated site and PDF artifacts, and run the existing regression suite.

**Tech Stack:** Markdown source, Node.js build scripts, Puppeteer PDF generation, Node `node:test`

---

### Task 1: Add AI Adoption subsection

**Files:**
- Modify: `content/cv.md`
- Verify output: `site/index.html`
- Verify output: `site/cv.pdf`

**Step 1: Update the source markdown**

Insert this subsection after `##### Cross-Team Impact`:

```md
##### AI Adoption
- Led AI adoption in the team by treating AI as a tool rather than a shortcut: focused first on preparing the engineering environment, workflows, and guardrails needed to avoid "garbage in, garbage out," while keeping human review and control over generated outputs.
- Drove adoption through education, practical enablement, and result-based work with skeptics, moving the team from near-zero usage to a state where ~75–80% of code is generated by agents under controlled workflows.
```

**Step 2: Rebuild generated outputs**

Run: `npm run build`

Expected: `Compiled site/index.html from content/cv.md` and `Generated site/cv.pdf`

**Step 3: Run verification tests**

Run: `node --test`

Expected: all tests pass
